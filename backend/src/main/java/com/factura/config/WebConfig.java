package com.factura.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Reserved for future web-level configuration (formatters/interceptors).
}
